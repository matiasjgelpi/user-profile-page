import { Open_Sans, Bona_Nova } from "next/font/google";

export const openSans = Open_Sans({ subsets: ["latin"] });

export const bonaNova = Bona_Nova({
  subsets: ["latin"],
  weight: ["400", "700"],
});
